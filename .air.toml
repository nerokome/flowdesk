# Config file for [Air](https://github.com/cosmtrek/air) in TOML format

root = "."
testdata_dir = "testdata"
tmp_dir = "tmp"

[build]
  args_bin = []
  # General binary path (fallback)
  bin = "./tmp/main.exe"
  # Point this specifically to your cmd/api folder
  cmd = "go build -o ./tmp/main.exe ./cmd/api/main.go"
  # Increased delay for OneDrive sync stability
  delay = 1500
  exclude_dir = ["assets", "tmp", "vendor", "testdata"]
  exclude_file = []
  exclude_regex = ["_test.go"]
  exclude_unchanged = false
  follow_symlink = false
  full_bin = ""
  include_dir = ["internal", "cmd", "pkg"]
  include_ext = ["go", "tpl", "tmpl", "html", "env"]
  include_file = []
  kill_delay = "0s"
  log = "build-errors.log"
  poll = false
  poll_interval = 0
  rerun = false
  rerun_delay = 500
  send_interrupt = false
  stop_on_error = true

  [build.windows]
    bin = ".\\tmp\\main.exe"
    cmd = "go build -o .\\tmp\\main.exe .\\cmd\\api\\main.go"

[color]
  app = ""
  build = "yellow"
  main = "magenta"
  runner = "green"
  watcher = "cyan"

[log]
  main_only = false
  silent = false
  time = false

[misc]
  # Clean up the exe when you stop Air
  clean_on_exit = true

[screen]
  clear_on_rebuild = true
  keep_scroll = true